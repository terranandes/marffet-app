# Code Review — 2026-02-18 v5
**Reviewer**: [CV]
**Scope**: Phase 17-A Grand Correlation (uncommitted changes)

---

## Modified Files (5 files, +317 / -250 lines)

### 1. `app/project_tw/calculator.py` — Split Double-Counting Guard

**Verdict**: ✅ **Approved**

```diff
 if yr_split > 1.0:
-    current_shares *= yr_split
+    # Check for overlapping stock dividend — avoid double-counting
+    has_stock_div = False
+    if dividend_data:
+        div_y = dividend_data.get(year) or dividend_data.get(str(year))
+        if div_y and float(div_y.get('stock', 0)) > 0.5:
+            has_stock_div = True
+    if not has_stock_div:
+        current_shares *= yr_split
```

- **Logic**: Correct. Stock = 0.5 threshold avoids ignoring tiny rounding artifacts while catching real stock dividends (typically ≥1.0).
- **Comment quality**: Excellent — explains WHY (ex-rights drops falsely detected as splits).
- **Risk**: Low. Only skips split when stock dividend > 0.5 exists in same year.

Also includes the **dividend timing fix** (去年留倉部位): dividends calculated before new investment. This was verified against MoneyCome's documentation.

### 2. `tests/analysis/correlate_all_stocks.py` — CAGR Year Alignment

**Verdict**: ✅ **Approved**

- Changed preferred CAGR key from `s2006e2025bao` to `s2006e2026bao` to match reference Excel column.
- Fixed deprecated `pandas .append()` → `pd.concat()`.

### 3. `docs/product/moneycome_methodology.md` — Table Correction

**Verdict**: ✅ **Approved (Boss-authored)**

- Renamed "Buy Price" → "Annual Capital Buy Price"
- Fixed Single mode: `bao/bah/bal` (was `bah/bal`)

### 4. `.gitignore` — Broader JSON coverage

**Verdict**: ✅ **Approved**

- Added `data/raw/*.json` for all raw JSON snapshots.

### 5. `docs/product/tasks.md` — Phase progress tracking

**Verdict**: ✅ **No issues**

---

## Outstanding Code Quality Issues

| # | File | Issue | Severity |
|---|------|-------|----------|
| 1 | `crawler.py:L850-855` | `權息` dividends zeroed out — data loss | 🔴 Critical |
| 2 | `crawler.py:L614-621` | Dead code after `return prices` (duplicate try/except block) | 🟡 Medium |
| 3 | `split_detector.py` | KNOWN_SPLITS dict has empty entries for TSMC — harmless but noisy | 🔵 Low |

---

## Recommendation

**Commit the 5 modified files** as a single Phase 17-A commit. The calculator fix and correlation script improvements are solid and well-tested. The `權息` parser bug in `crawler.py` should be tracked separately as a dedicated task.
