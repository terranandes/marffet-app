# Code Review: Mars Strategy Logic Unified
**Date:** 2026-02-12
**Reviewer:** `[CV]` (AntiGravity Code Verification Agent)
**Component:** Backend Core Logic

## 1. `app/services/roi_calculator.py`
- **Logic:** Verified `calculate_roi` correctly handles compound annual growth rate for multi-year simulations.
- **Split Handling:** Strong. The explicit `_check_splits` method ensures shares are correctly diluted/multiplied based on historical actions.
- **Observation:** Ensure decimal precision is consistent across rounding steps to avoid penny discrepancies in long-term total wealth.

## 2. `app/services/strategy_service.py`
- **Integration:** Good use of `MarketCache`. The dynamic start-year filtering is efficient.
- **Data Patching:** The merge logic between `yfinance` history and `dividend_patches.json` is robust. It correctly prioritizes manual patches for cases where TWSE data is missing or erroneous.
- **Improvement:** Consider caching the intermediate `df` per run to speed up re-analysis of the same stock in a single session.

## 3. `app/services/market_cache.py`
- **Singleton Pattern:** Correctly implemented.
- **Warming Up:** The `get_prices_db` loop is straightforward.
- **Critical Note:** The reliance on `Market_YYYY_Prices.json` files means the system is only as good as the raw data. The recent repair of 2000-2023 data was essential.

## 4. Overall Code Quality
- **Naming:** Consistent and self-documenting.
- **Error Handling:** Improved with the removal of hardcoded paths and addition of explicit skips for empty dataframes.
- **Rating:** **High**. The logic is significantly cleaner than the Legacy V1 implementation.

---
**Review recorded by:** `[CV]`
