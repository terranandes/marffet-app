# Meeting Notes: 2026-02-20 v2 (Agents Sync & Correlation Debugging)

**Date**: 2026-02-20
**Attendees**: [PM], [SPEC], [PL], [CODE], [UI], [CV]
**Topic**: TPEx Historical Data Truncation & Calculator Logic Fixes

## 1. Project Live Progress (Task Update)
- **Phase 17**: Grand Correlation debugging is intensely active.
- **Current State**: The match rate dipped from 71% to 31% after deploying the native TPEx boundary crawler.

## 2. 🚨 System Discoveries & Bug Triages

### 🔴 BUG-116-CODE: TPEx Native API Silent Date Fallback (Data Corruption)
- **Reporter**: [CV] & [CODE]
- **Issue**: The official TPEx `stk_quote_result.php` completely ignores date parameters prior to ~2024. When queried for 2006 bounds (`d=95/01/02`), it successfully responded with HTTP 200 but silently returned **2026** prices.
- **Impact**: All historical TPEx limits (2006-2025) were cloned with modern data. Hundreds of OTC stocks registered exactly **0.0% CAGR**, artificially tanking the correlation rate.
- **Resolution**: [CODE] will revert `TPEXCrawler` to use YFinance historical batch fetching. The native TPEx API can only be trusted for near-term operations.

### 🟢 FIX: Calculator Pre-IPO Cash Destruction 
- **Owner**: [CODE]
- **Issue**: `ROICalculator.calculate_complex_simulation` was dropping the initial `principal` and cumulative `annual_investment` for late-listing stocks (e.g., 3135 listed in 2014) because the loop skipped when `p_action == 0`.
- **Fix Applied**: [CODE] rewrote the loop to utilize an `accumulated_cash` vault. Cash now pools during pre-IPO years and drops seamlessly onto the market during the first valid trading year, precisely mirroring the MoneyCome Reference architecture.

### 🟡 SPEC: Delisted Stock Terminal Math
- **Owner**: [SPEC]
- **Insight**: For bankrupt stocks (like 1606 Kolin), the Reference calculates a crushing terminal CAGR (e.g., -58.0%). Mathematical proofs indicate this relies solely on total dividends salvaged over its active lifespan relative to invested capital.
- **Action Required**: [CODE] must ensure `calculator.py` correctly halts tracking post-bankruptcy rather than applying the standard 20-year division.

## 3. Worktree & Operations
- Hanging DuckDB connections were forcefully terminated via `fuser` and process signals to restore DB availability.
- The `continue_and_report.sh` script is active but paused for debugging.

## 4. Next Steps
1. **[CODE]**: Down-shift the TPEx crawler back to the YFinance batch loader to restore authentic historical bounds and re-run `recovery_twse_bounds.py`.
2. **[CODE]**: Re-execute the correlation analysis to verify if the Pre-IPO cash fix pushes the Match Rate above the previous 71% peak.
3. **[PL]**: Continue purging the YFinance Adjusted Dividend mismatch (BUG-115) from the DuckDB instance.

---
*Generated by [PL] via AntiGravity per /agents-sync-meeting workflow*
