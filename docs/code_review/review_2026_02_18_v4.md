# Code Review: Phase 17-E Commits + Regression Analysis
**Date**: 2026-02-18 15:40 HKT
**Reviewer**: [CV]
**Scope**: 3 new commits since v3 review (`2091e16`, `a76b0cb`, `3dec170`)

---

## Review Summary

| Commit | Files | Verdict |
|--------|-------|---------|
| `2091e16` (Phase 16/17 housekeeping) | 17 files, +1209/-31 | ✅ Approved |
| `a76b0cb` (Indentation + pd.isna fix) | 7 files, +82/-15 | ⚠️ Approved (tooling issue noted) |
| `3dec170` (Critical pd.notna restore) | 1 file, +3/-3 | ✅ **Critical Fix** — Approved |

---

## Detailed Findings

### 1. `market_data_service.py` — Code Quality Fixes

**Changes reviewed (across 3 commits)**:
- ✅ Redundant `SAVE_INTERVAL` at L743, L755 → Commented out (correct: L832 is the authoritative one)
- ✅ Debug prints at L771, L873 → Commented out
- ✅ Bare `except:` at L909 → `except Exception as e:` with logging
- ✅ Bare `except:` at L919 → `except Exception as e:` with comment
- ✅ `pd.isna` restored at L902 (was broken by first edit attempt)
- ✅ Indentation fixed at L909-912

### 2. 🔴 CRITICAL: `pd.notna` Regression (POST-MORTEM)

The `multi_replace_file_content` tool **silently modified lines outside the edit range**, changing `pd.notna(` to `pd.na(` at 3 locations (L343, L460, L506).

**Root Cause**: The edit tool's fuzzy matching or diff application corrupted unrelated lines when processing a large file with multiple similar patterns.

**Impact Assessment**:
- These lines are in `update_price_cache_monthly()` and `_fetch_yfinance_monthly()` — used for monthly price cache updates
- If these functions were called, valid price data would be **skipped** (filter logic inverted)
- **No data damage** occurred because these functions were not called during Phase 16/17 work
- Fixed in commit `3dec170`

**Recommendation**: After any `multi_replace` on critical files, run:
```bash
grep -n "pd\.na(" <file>    # Should return 0 results
grep -n "pd\.notna(" <file>  # Verify expected count
```

### 3. `correlate_all_stocks.py` — Phase 17-A Script

| Issue | Severity | Status |
|-------|----------|--------|
| Wrong import: `from app.services.mars_strategy` | 🔴 Blocking | Open |
| Correct path: `from app.project_tw.strategies.mars` | — | Needs fix |
| `ROICalculator` import needs correction | 🔴 Blocking | Open |
| CAGR calculation should use `calculate_complex_simulation` | 🟡 Medium | Needs alignment |
| Missing `openpyxl` dependency check | 🟢 Low | Add try/except |

**Verdict**: ⚠️ **Needs rework before execution**.

### 4. `.gitignore` Coverage

| Pattern | Status |
|---------|--------|
| `data/raw/mi_index/*.json` | ✅ Covered |
| `data/raw/Market_*.json` | ❌ **Not covered** (20 files) |
| `data/raw/TWSE_Dividends_*.json` | ❌ **Not covered** (22 files) |
| `data/raw/TPEx_*.json` | ❌ **Not covered** (41 files) |
| `data/raw/ListingDates.json` | ❌ **Not covered** (1 file) |

**Recommendation**: Replace `data/raw/mi_index/*.json` with `data/raw/*.json` to catch all raw snapshots.

---

## Outstanding Code Quality Debt

| Issue | File | Status |
|-------|------|--------|
| `pd.notna` regression | `market_data_service.py` | ✅ FIXED (`3dec170`) |
| f-string SQL (ops scripts) | Various | Low — non-production |
| Dead class in benchmark | `benchmark_mars_simulation.py` | Low — test script |
| `supplement_splits.py` UPSERT edge | `supplement_splits.py` | Documented |

**Total Production Issues**: **0** (all resolved ✅)

---

**Signed**: [CV] Code Verification Agent
