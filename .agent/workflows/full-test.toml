description = "Full coverage tests on local and remote Zeabur."
prompt = """
Use skill `using-git-worktrees` to do
1. Create a worktree with current/specified commit and branch. <br>
   Notice un-tracked git files but neccessary, for exemple: `./.env`, please copy it as well into the worktree
2. Update rigorous test-plan (./product/test_plan.md) according to current SPEC.
3. According to the latest test plan do:
- Adjust/Add testing programs to verify
4. Use skill `agent-browser`/`webapp-testing` to
- Full headlessly exhausted bug-hunting with MCP Playwright Suite on local and remote Zeabur.
- File the new bug found at `./jira`.You must emphaseize which agent is firing tickets.
- Take screenshots of evidences with Playwright at `./tests/screenshots`. Don't commit/push screenshots.
"""
