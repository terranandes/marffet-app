# Meeting Notes: 2026-02-20 Final Sync-Up

**Date**: 2026-02-20
**Attendees**: [PM], [SPEC], [PL], [CODE], [UI], [CV], [Terran/Boss - Async Review]
**Topic**: Comprehensive Agents Sync-Up

## 1. Project Live Progress
- **Phase 17**: Grand Correlation debugging is the current focus.
- **Current State**: System is fully running on DuckDB. However, data truncation from TPEx and YFinance dividend mismatches have actively dropped the Match Rate below target. Operations are underway to recalibrate historical boundaries.

## 2. Bug Triages & Jira
- **🔴 BUG-116-CODE**: TPEx Native API Silent Date Fallback processing. Causes 0% CAGR for older bounds.
- **🔴 BUG-115-PL**: YFinance Adjusted Dividend Mismatch. A severity HIGH data corruption issue currently being purged from DuckDB.
- **🟡 BUG-114-CV**: Mobile Portfolio Card Click Timeout. E2E tests indicate the card is not visible in the mobile viewport. Requires [UI] attention.

## 3. Features & Deployment Status
- **Implemented**: Universal Data Lake (DuckDB), In-Memory Caching (`MarketCache`), Pre-IPO cash pooling logic in calculator.
- **Planned / Deferred**: Interactive Backfill Dashboard, Mobile Premium Overhaul (deferred to stabilization).
- **Deployment Discrepancy**: Zeabur deployment lacks a persistent volume mount (`/data`), meaning daily `market.duckdb` updates are wiped upon deployment. Fixing this is a critical remote blocker.

## 4. Worktree & Branch Status
- Branch is `master`, ahead by 1 commit.
- Uncommitted changes include debugging tools, recovery scripts (`recovery_twse_bounds.py`, `recovery_yfinance_fast.py`), and test outputs.
- Background Python jobs are actively running DuckDB connections for data recovery.

## 5. UI/UX & Mobile Web Layout Review
- The alignment of the Next.js migration is complete, but BUG-114 indicates that the mobile card expansion logic or CSS needs adjustment to fit within viewport bounds gracefully. [UI] will audit the mobile structure.

## 6. Multi-Agent Brainstorming & Adjustments
- **[PM]**: The singular priority remains cracking the >85% correlation match rate. Features are frozen until data correctness is absolute.
- **[SPEC]**: We must enforce structural rigor in `calculator.py` for bankrupt/delisted stocks to match MoneyCome's terminal math exactly.
- **[CODE]**: Abandoning native TPEx API for historical bounds; rewinding to YFinance batch loader. Will commit the pre-IPO accumulated cash fix today.
- **[CV]**: Automation scripts (`continue_and_report.sh`) are brittle due to PID locks. Advise transitioning to file semaphore flags.
- **[PL]**: We will back up DuckDB to separated Parquet files (`data/backup/prices_YYYY.parquet`) and commit those to Git to bypass the Zeabur volume limitation temporarily.

## 7. Next Actions
- [PL] Report summary to Terran.
- [CODE] Execute YFinance recovery and re-run correlation.
- [UI] Audit mobile portfolio card dimensions.

---
*Generated by [PL] via AntiGravity per /agents-sync-meeting workflow*
