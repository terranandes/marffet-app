# Code Review: 2026-01-24

**Reviewer:** [CV] / [CODE]  
**Focus:** Guest Mode & Admin UI Refactor

## 1. Portfolio Service (`frontend/src/services/portfolioService.ts`)
- **Factory Pattern**: Good use of factory to switch between `ApiAdapter` and `guestAdapter`.
- **Auth Detection**: Switching to probing `/api/portfolio/targets` is clever and reliable, as it forces an auth check on the backend.
- **Typing**: Interfaces `IPortfolioService` ensure consistency.

## 2. Admin Page (`frontend/src/app/admin/page.tsx`)
- **UI Consistency**: The use of `grid-cols-3` for metrics aligns perfectly with the subscription section.
- **Styling**: Tailwind classes `bg-white/5` and border colors match the dark theme well.

## 3. E2E Suite (`tests/e2e_suite.py`)
- **Robustness**: Using `get_by_role` is much better than text matching for generic buttons like "Create".
- **Waits**: `time.sleep` is used sparingly but effectively for API waits. Consider `expect(locator).to_be_visible()` for even more stability in future.
- **Coverage**: Covers critical path (Create Group -> Add Stock -> Transaction).

## 4. General
- **Dependencies**: `uuid` added to `package.json` resolves the build error.
- **Linting**: Relative imports in `page.tsx` fixed the `@/` alias resolution warning/error.
